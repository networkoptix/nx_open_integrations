<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>NxVirtualCameraUpload</RootNamespace>
    <AssemblyName>nx-virtual-camera-upload</AssemblyName>
    <!-- No NuGet packages: HttpClient + System.Text.Json + System.Security.Cryptography are in the framework. -->
  </PropertyGroup>

</Project>
