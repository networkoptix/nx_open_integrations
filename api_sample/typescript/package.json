{
  "name": "nx-api-samples-typescript",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "TypeScript versions of the Nx API samples. Run directly on Node 22.6+ via native type stripping — no build step. The only dependencies are dev-only (typescript + @types/node) for type-checking.",
  "scripts": {
    "test": "node --test \"**/test_*.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "@types/node": "^22.0.0"
  }
}
